# --- Core ---
requests>=2.31.0
PyYAML>=6.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # parquet 캐시 저장 엔진 (없으면 pickle 로 자동 폴백)

# --- Data / Universe ---
pykrx>=1.0.45            # KOSPI/KOSDAQ 종목 리스트 및 일봉 백업 소스
finance-datareader>=0.9.50

# --- Backtest / Analysis ---
matplotlib>=3.7.0
tqdm>=4.66.0

# --- Optional (스케줄링) ---
schedule>=1.2.0
